\begin{lstlisting}
class YellingActor(out: ActorRef) extends Actor {
  def preStart() = println("Connected")
  def postStop() = println("Disconnected")
  def receive = {
    case message: String =>
      println("Recived: " + message)
      out ! message.toUpperCase
  }
}
\end{lstlisting}
